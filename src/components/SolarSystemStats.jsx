import React, { useState } from 'react';
// import CosmicOverviewModal from './CosmicOverviewModal';
// import infoIcon from '../assets/textures/icons8-info-32.png';

// All icon button and modal logic removed

const SolarSystemStats = ({ isSidebarOpen, isAnyInfoComponentOpen }) => {
  // Hide the button when sidebar is open OR when any info component is open
  // (No button to show now)
  return null;
};

export default SolarSystemStats; 